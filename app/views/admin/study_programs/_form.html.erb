<%= simple_form_for([:admin, @study_program]) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :title %>
    </div>

    <div class="form-actions">
      <%= f.button :magic_submit, cancel: admin_study_programs_path %>
    </div>
<% end %>
